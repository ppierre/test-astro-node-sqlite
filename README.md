Installé avec Bun : `bun i` (mais `npm i` doit aussi aller si pas Bun...)

La base de données n'est pas dans le `.gitignore` !

Si supprimé, il y a la recréation des tables.

Il y aussi un script 'seed' : `npm run seed`
